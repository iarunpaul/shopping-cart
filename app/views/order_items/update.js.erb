$("#card-link").html("<%= j link_to "Your Cart has #{current_order.order_items.size} with subtotal #{current_order.grand_total}", card_path %>");
$("#show-tbody").html("<%= j render 'cards/order_items' %>");
